autoindex off;
disable_symlinks on;
if ($uri !~ \.md$) { return 404; }
# Keep source files inert even if a future docs/ file ends in .html.
types { }
default_type text/plain;
charset utf-8;
limit_except GET { deny all; }
