# 2026-09-17 安全修复上线

2026-09-17 完成安全修复发布。四个仓库的源码已推送 GitHub `main`，[生产站点](https://agent-communication.online) 已更新 Web、Platform 和 nginx。修复内容见 [安全检查记录](../verification/SECURITY_REVIEW_2026-09-16.md)。

## 公开源码版本

| 仓库 | 本次应用版本 |
| --- | --- |
| Deploy | `2a073ad51eed0725256f5cc58c72c6ade9477a13` |
| Web | `7ff8b548454359a2e2751d2444d94243dd796b8d` |
| Platform | `7b729a366c9868df9b9fd18a1ee50a517bdbad84` |
| SDK | `941ee23f55832c542038281bc6dd3876829d9b54` |

后续根仓库发布记录提交只更新文档，不改变上述应用版本及子模块引用。

正式 Dockerfile 重建通过，运行版本为 Node 24.21.0、Next 15.5.25、React 19.3.0、Go 1.26.8、Alpine 3.24.1 和 nginx 1.31.6。Compose 固定已核验的 nginx 官方摘要。正式 npm 11.19.0 检出的两个可选依赖锁文件缺项已补齐，`npm ci` 与生产构建通过，安装审计为 0 项漏洞。

## 验收结果

- 使用独立合成账户验证旧 bcrypt 登录升级 scrypt、长密码错误后缀拒绝、新账户注册、再次登录及重启保留会话。
- 匿名私有接口返回 401，跨账户访问返回 404，认证体积和多余路径段限制通过；管理员令牌只接受请求头。
- 候选页面引用的 23 个静态资源通过，数据库完整性及服务进程降权验证通过。
- 上线后首页、登录页和健康接口返回 200，13 个登录页资源通过，HTTPS 证书和安全头检查通过。
- 伪造转发头仍受认证限流约束，超限返回 429；后台同步恢复，三个服务稳定运行。
- 上线前已备份数据，原账户、加密密钥和数据卷保留。

本次更新服务器服务和公开源码。已有早期安装包及用户已安装的客户端没有自动升级，需单独更新。SDK 上游 DHT 漏洞仍无修复版本，本次源码默认禁用 DHT；显式启用及旧客户端不在服务端更新的修复范围内。
