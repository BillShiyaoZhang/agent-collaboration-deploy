# 2026-09-26 Web 聊天与合作工作流

本次按[聊天与合作设计](../design/WEB_CHAT_AND_COLLABORATION.md)交付 P0/P1：人可以直接和自己的 agent 对话，通过联系人和目标表单发起合作，在原对话与合作列表判断进展并完成明确的审批。

**状态：实现与本地验收阶段，尚未切换生产。** 固定提交、生产镜像、正式 SDK 资产和云端验收将在实际完成后补充。本记录不代表已安装 agent 自动升级。

## 交付范围

- 聊天、合作、联系人、我的四个入口；单 agent 直接进入聊天，多 agent 从聊天列表选择。
- 格式化回复、复制与引用、会话标题、归档、保存范围内的搜索、草稿及滚动位置；切换会话先保存旧草稿，失败保留原界面。
- 目标、成功条件、联系人、时间范围、资源和预算的合作表单；沿用准备事项、范围审批、邀请、接收回执和独立有限 worker 授权。
- 合作卡显示目标、状态、参与方、最后变化、授权范围与下一步。仅有邀请发出、传输回执或 agreement 尚未同步时不能显示业务完成。
- 真实来源关联原对话与相关事项；自然语言中的 ID 不构成可信关联，也不产生任何额外授权。
- 普通聊天完成、失败和中断的持久提醒；查看原对话才推进已读，查看事项页不消除聊天结果提醒。
- 服务端持久操作记录和稳定请求 ID；未知写入结果只核对原操作，过期且可能重复的写入不能通过通用“重新提交”生成新请求。
- SDK runtime 0.1.8 / Hermes connector 1.5.10（v0.9.3）的可信来源、历史范围和聊天结果提醒支持。旧安装继续按已声明的能力工作。

P2 的流式回复、附件、日历执行和更完整的协作会话留作后续实现；当前有限 worker 的暂停、关闭及授权有效期按实际运行时语义显示。暂停本机 worker 不表示对方已停止。平台策略、用户身份和配对继续沿用现行配置。

## 本地验证

在 Windows 隔离 fixture、临时 SQLite 数据库与真实 Hermes 依赖环境中验证；不修改用户已有 Hermes profile 或 Gateway。

| 检查 | 实际结果 |
| --- | --- |
| SDK runtime | 176 项通过 |
| Hermes connector | 143 项通过，远程生命周期补充 17 项通过 |
| 共享客户端契约 | 15 项通过 |
| 根发布工具 | 5 项通过 |
| 早期试用安装与发布工具 | 44 项通过 |
| 云端备份/验收脚本 | 12 项通过，兼容服务器 Python 3.6 |
| Web 单元与共享逻辑 | 243 项通过 |
| Next 生产构建 / TypeScript | 通过 |
| 实际生产镜像 | 13 项通过：非 root 网络服务、旧库增量迁移、身份保留、HTTP 与匿名 API |
| 工作台浏览器 | 9 项通过，含跨设备草稿、迟到 GET、窄屏；页面异常为 0 |
| 断线和进程重启 | 3 项通过，恢复历史且不重复发送 |
| 可访问性浏览器 | 44 项通过 |
| 合作产品故事、消息与提醒回归 | 最终结果待验收完成后补充 |

Hermes 相关测试使用实际安装的解释器依赖与独立测试目录；公开包的构建与发布还必须以正式 CI 成功和资产校验为准。

## 云端备份与切换

切换前在线备份已于 2026-09-26 04:41:33 UTC 完成，保存于服务器受限目录：

`/root/agent-comm-releases/2026-09-26-chat-collaboration/pre-cutover/`

四库各自的在线快照 `quick_check=ok`。它们是独立一致性快照，不是跨四库的同一事务。切换前 User 46、Agent 43；原身份、账户和凭证字段摘要已记入只保存在服务器的私有基线。受保护的 19 份配置/身份文件已备份，原签名策略 epoch 3、compliance、allow_v1=false。

切换将使用本地构建的 Linux amd64 Web 镜像，不在小内存生产服务器执行 Next 构建。只重建 Web，并检查/reload nginx；Platform 镜像和容器不替换。原持久数据卷保留，数据库只做添加表和索引的增量迁移。

## 固定版本与 GitHub 发布

| 对象 | 固定值 |
| --- | --- |
| Deploy 发布源码 | `d00abafbf926752d7a84fb1b3e62ebf6d137dbe8` |
| Web 运行源码 | `bec1c0da400bdc0ae7d1275402dd2bbcab231540` |
| Platform 固定引用 | `8382a74a881ded7d46241c9333ac06bb55fe078d` |
| SDK v0.9.3 | `fb7fb916945a5c11c219c517f7ff20a1b2856105` |
| Web 镜像 ID | `sha256:1d31745c3e7102f4e942e1f4681ef41aeee49329a10d4a852f3b0878dc908f98` |
| 镜像压缩包 SHA-256 | `ef9284decd390d78d9a1b6f88043ff707e1a16ba3f65fff5cb1d1cdf02bfdaa1` |
| 源码快照 SHA-256 | `ad0a1dc679ee8a3dc3dcd72bda67e9f313dd1a560b147ae55457db48118c9c8d` |

四仓实现已推送至 GitHub main，并保留 `codex/web-chat-collaboration` 分支。镜像从提交中的 Git blob 快照构建，镜像标签记录 Web revision；后续纯测试/验收文档提交不改变这个应用镜像。

[正式 CI](https://github.com/BillShiyaoZhang/agent-comm/actions/runs/36219401522)全部通过，[v0.9.3](https://github.com/BillShiyaoZhang/agent-comm/releases/tag/v0.9.3)于 2026-09-26 05:01:22 UTC 正式发布 16 项资产。本机下载并校验全部资产；四平台安装 ZIP 的内部文件、wheel 与发布信任资料以及源码 ZIP 的固定引用均通过。官网五 ZIP 和正确的 `early-access-manifest.json` 已上传受限暂存目录，尚待下载目录切换。交叉编译与包校验不代表已在四种系统上分别运行 Hermes。

## 实际生产切换与公网验收

待完成生产切换与只读验收后补充。

## 回滚

保留旧 Web 镜像 `sha256:30cee9fb1e0db7269d778836ab23a30d36c0fbb1c9e9be0a709204f990b71419` 与切换前受限备份。应用回滚须同时核对版本适用的公开文档和 SDK 下载清单；新增数据库表可保留。数据库恢复需要单独的维护窗口，不能覆盖切换后产生的用户数据。
