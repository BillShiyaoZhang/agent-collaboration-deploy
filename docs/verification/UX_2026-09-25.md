# 2026-09-25 双 Agent 用户体验验收进度

本记录对应[真人逐段执行卡](../testing/UX_HITL_2026-09-25.md)。结论只覆盖下述实际操作、环境和当时的线上版本；“页面已受理”、Agent 本机入队、对端持久收件、真人阅读和最终业务决定分别判定。没有发生的线上部署、自动浏览器复测或双边协作操作均不记为通过。

## 环境、角色与证据

真人运行号 `live-20260925-ux-hitl-01`。同一位测试者依次扮演 Alice 与 Bob；两端是本机隔离的 Windows Hermes profile、helper 身份、端口与 Store，并连接生产 HTTPS Platform/Web。Alice helper 端口 `49707`、Agent URN `urn:agent-comm:agent:7mYCMiYA5Mihgo6NGhyyDj`；Bob 端口 `57747`、URN `urn:agent-comm:agent:4DyLskBe9bugc8vPHLDauq`。网页账户和浏览器分别核对，Alice 使用 Chrome，Bob 使用 Edge；曾发现 Alice Chrome 显示 Bob 账户，测试者纠正并核对账户后才领取 Alice 的一次性连接。两端使用正式 v0.9.1 Windows helper，分别完成 Web 披露确认、一次性连接领取，并由测试者本人分别执行本机精确合规策略授权。两边工作台各只显示自己的连接，测试者报告连接正常、未看到另一方资料。

本轮策略为已验签 `compliance` epoch 3，摘要 `6f9f7bdf26c5e7761cbe8c451a22fd11f9a448d912fa5bc3ae61c1e53f3ff5c4`，`allow_v1=false`。Web 确认与本机授权是两次独立决定。结果、脱敏索引和一次性测试数据在忽略提交的 `build/live-two-agent-acceptance/live-20260925-ux-hitl-01/`；其中包含私有配置，**不得整体提交或复制到公开报告**。本文件只列测试用 URN、业务 ID 和去敏结论。

| 用例 | 预设效果 | 真人操作与实际观察 | 截至本记录的判定 |
| --- | --- | --- | --- |
| T03/T20 接入 | Alice/Bob 独立安装、确认披露、领取各自连接；账户之间不泄漏资料 | 两人分别注册、领取自己的连接并检查可用；Bob 表示披露文案清楚。两个本机精确策略授权由测试者逐端执行。 | Windows 双端接入与账户隔离部分通过；T20 的其他 OS、错误包、开机自启等未由本轮覆盖。 |
| T18 披露和受管控制 | 两账户分别确认当前策略，并可各自暂停/恢复 Web 控制 | 两账户的 Web 披露和本机授权完成。 | 披露确认完成；暂停/恢复、旧副本和交叉影响未执行。 |
| T04 首轮拒绝 | Alice 申请，Bob 独立拒绝，两端不形成连接 | Alice Web 发起 `friend-c9c2753e15ee311bbe9c1d61b2f9593584d9f992`，Bob 本人拒绝；Alice 看到“已拒绝”，未显示已连接，认为文案清楚。 | 拒绝路径通过。 |
| T04 拒绝后再申请 | Alice 沿用同一准确 URN 重新申请，使用独立新请求；Bob 再独立接受 | Alice 在 Web 新建第二个同 URN 联系人返回 `invalid_params`。改由 Bob 发起反方向独立请求 `friend-5f40d4d9fa5ccdc441adba95d5ac207497bedfa9`，Alice 本人接受；两端最终 `connected`。 | 原定 Alice 重申请路径失败；反方向替代路径通过，不能抵消原缺陷。 |
| T05 Alice Web → Bob 原生 → Alice Web | Bob 原生 Hermes 看见准确来信和提醒，独立回复；Alice Web 按准确消息 ID 阅读并关闭提醒 | Alice Web 发 `message-783190c9-bc10-4882-8f78-a7cba00ee36a`。Bob 原生 Hermes 本人看到准确来信；通过原生问题卡 `approval-bfcf1c753359874892a19010048a720c` 本人确认，回复 `t05-b-ack-bob-001` 到达 Alice。Bob Web 将 Alice 原信标为已读、Agent 已记录；Alice Web 最终也将 Bob 原生回复标为已读。 | 这一路真人操作和收件状态完成；曾发生卡片识别歧义，详见下文。 |
| T05 Bob Web → Alice 原生 → Bob Web | Alice 原生 Hermes 看见第二方向的来信并回复，Bob Web 阅读该回复 | Bob Web 发 `message-93c01ca4-0b2f-4a87-b5fa-8cf984caeebf`；Alice Web 看见并标为已读。 | 传输与 Alice Web 可见完成；Alice 原生阅读/回复、Bob 再阅读尚未执行，不能判定整条路径通过。 |
| T06 双边协作 | 两端独立 scope、invite/join、同版各自接受或独立拒绝；不创建日历 | 真人尚未操作 T06。隔离 Alice venv 中 `python/tests/test_collaboration_v2.py` 的 29 项本地协议测试通过。 | 本地协议检查通过；生产双边及真人体验未测试。 |
| T07 真实模型对话 | Web 提交后原 turn 最终 `completed`，回复只用一句中文并含指定标记 | Alice 本人发送。原 turn `turn-5bb3ebbe0f6078dd7ae64d0d286e86e9e663a65f`、会话 `85957f6b-ee37-4218-82a0-2ffe1309816a` 在 Alice 本机 Store 为 `completed`、回复非空且无错误；测试者看到最终回复含 `T07-A1-7mYCMiYA` 并报告页面显示已完成。回复包含额外个人画像建议，且超过要求的一句。 | 真实模型回合完成；提示遵循/答复质量有偏差。测试者未单独评价状态变化是否易懂。 |
| T10 系统通知 | Bob 启用后台 Push，收到固定、无私信正文的 Windows 通知；点击只进入提醒中心 | Bob 本人在 Edge 启用后台推送、点击“发送测试提醒”，亲眼看到系统通知；点击进入提醒中心。测试者确认通知没有私人消息正文，也没有自动接受任何请求。 | 真人系统通知段通过；320/390px 真人窄屏、键盘体验和真实手机未执行。 |

T04 失败的直接原因是 Agent 对已拒绝联系人的准确 URN 保留绑定，而当时生产 Web 只提供“新增联系人”，新建另一个 `contact_id` 会触发参数校验失败。正确重申请需要从原已拒绝联系人发起，沿用原 `contact_id`、别名和 URN，再产生独立的新好友请求。Web 的重申请入口和提示正在修复；**截至本记录，不能宣称生产 Web 已部署该修复，也不能宣称线上重申请复测通过**。反方向发起后的 `connected` 仅用于继续 T05，不证明这处 Alice UI 缺陷已消失。

T05 的两条 Bob→Alice 消息时间相近、来自同一个 URN。Alice 曾报告已将原生回复 `t05-b-ack-bob-001` 标为已读，但随后按本机 Store 的消息 ID 对账，实际先读的是较新的 Bob Web 消息 `message-93c01ca4-0b2f-4a87-b5fa-8cf984caeebf`，原生回复当时仍未读。测试者刷新后只点击原生回复自己的按钮；最终**两条均已读，各自提醒均已解决**。目前证据说明卡片区分不够直观；没有证据表明 Web 把点击的一条错误写到另一条消息 ID。修复方向是显示可复制的精确消息 ID，并让已读按钮的可访问名称包含该 ID，再用测试核对 `inbox.mark_read` 的参数。

## 之后的隔离自动浏览器阶段

测试者随后明确要求 Codex 自己执行后续流程，并按“先预设效果，再测试看是否符合预期”推进。因此从这一时点起，自动动作限定在 **Codex 新建、持有的隔离合成账户、浏览器 profile 和 Agent 身份**；其结论标为自动化功能或界面机制验证，不倒填为上述真人 Alice/Bob 的决定、亲眼所见或主观理解。自动判据在继续测试前写入忽略提交的 `build/live-two-agent-acceptance/live-20260925-ux-hitl-01/automated-expectations.md`。桌面内置 Computer Use 控制因无法可靠验证当前浏览器 URL 而停止；后续浏览器入口采用隔离 Playwright Chromium，不能称为内置浏览器已通过。

自动运行号 `live-20260925-codex-web-auto-02`：两套全新 Windows Hermes/helper profile、独立 Store 和 Web 账户，helper 分别监听 `63525`、`63546`；Alice URN `urn:agent-comm:agent:GNQW7XrCMGctnZbbvM3Ew8`，Bob URN `urn:agent-comm:agent:QPjQsp3x2f3JNbtXhsy9Sy`。隔离 Playwright Chromium 已为两账户完成生产 Web 注册、策略确认和一次性连接领取，`browser-claim.json` 中两端均为 `policy_verified=true`、`claim_completed=true`。随后分别读取已验签的精确 epoch 3 披露并只对这两个 Codex 持有的身份执行本机授权，读回均为 `v2_send_ready=true`。密码、票据、模型密钥和浏览器存储状态只存放在忽略提交的私有文件，不在此报告披露。**这仅是自动首联结果，不是下表其他用例已通过。**

| 后续用例 | 预先固定的客观判据 | 自动结果 / 证据索引 | 线上版本与结论 |
| --- | --- | --- | --- |
| T04 拒绝后同绑定重申请 | 拒绝态才显示入口；先确认准确 URN，再沿用原绑定只提交一次；旧拒绝、新请求各自保留，外部旁路请求不能消除不确定状态 | 待填写 | 待部署修复后测试 |
| T05 双向消息和精确已读 | 双方消息有唯一 ID、准确发送/接收者及持久收件；对一个 ID 标已读不影响另一条 | 待填写 | 未执行自动双向验收 |
| T06 双边协作 | 双方独立 scope 和决定；同版双方接受才成约；独立拒绝不成约；无日历写入 | 本地协议 29 项通过；生产双边待填写 | 本地结果不能替代线上流程 |
| T07 模型与刷新 | 原 turn 完成、回复非空、刷新后不重复执行 | 真人原 turn 已完成；自动恢复场景待填写 | 不把真人结果当自动恢复验证 |
| T10 窄屏、键盘和通知导航 | 320/390px 无横向溢出，键盘焦点和操作有效；通知点击只到提醒中心 | 真人系统通知已通过；自动窄屏/键盘待填写 | 不把自动布局检查写成真人体验 |

任何后续修复、提交、部署、重测都应在本表补实际执行时间、代码/镜像版本、消息或操作 ID、通过/失败与证据路径；不能仅因源码测试通过就把生产用例改判通过。
