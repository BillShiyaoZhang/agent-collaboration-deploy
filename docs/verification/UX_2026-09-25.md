# 2026-09-25 双 Agent 用户体验验收进度

本记录对应[真人逐段执行卡](../testing/UX_HITL_2026-09-25.md)。结论只覆盖下述实际操作、环境和当时的线上版本；“页面已受理”、Agent 本机入队、对端持久收件、真人阅读和最终业务决定分别判定。后来的生产 Web 部署与隔离合成测试单独记录，不能倒填为此前真人操作的结果。

## 环境、角色与证据

真人运行号 `live-20260925-ux-hitl-01`。同一位测试者依次扮演 Alice 与 Bob；两端是本机隔离的 Windows Hermes profile、helper 身份、端口与 Store，并连接生产 HTTPS Platform/Web。Alice helper 端口 `49707`、Agent URN `urn:agent-comm:agent:7mYCMiYA5Mihgo6NGhyyDj`；Bob 端口 `57747`、URN `urn:agent-comm:agent:4DyLskBe9bugc8vPHLDauq`。网页账户和浏览器分别核对，Alice 使用 Chrome，Bob 使用 Edge；曾发现 Alice Chrome 显示 Bob 账户，测试者纠正并核对账户后才领取 Alice 的一次性连接。两端使用正式 v0.9.1 Windows helper，分别完成 Web 披露确认、一次性连接领取，并由测试者本人分别执行本机精确合规策略授权。两边工作台各只显示自己的连接，测试者报告连接正常、未看到另一方资料。

本轮策略为已验签 `compliance` epoch 3，摘要 `6f9f7bdf26c5e7761cbe8c451a22fd11f9a448d912fa5bc3ae61c1e53f3ff5c4`，`allow_v1=false`。Web 确认与本机授权是两次独立决定。结果、脱敏索引和一次性测试数据在忽略提交的 `build/live-two-agent-acceptance/live-20260925-ux-hitl-01/`；其中包含私有配置，**不得整体提交或复制到公开报告**。本文件只列测试用 URN、业务 ID 和去敏结论。

| 用例 | 预设效果 | 真人操作与实际观察 | 截至本记录的判定 |
| --- | --- | --- | --- |
| T03/T20 接入 | Alice/Bob 独立安装、确认披露、领取各自连接；账户之间不泄漏资料 | 两人分别注册、领取自己的连接并检查可用；Bob 表示披露文案清楚。两个本机精确策略授权由测试者逐端执行。 | Windows 双端接入与账户隔离部分通过；T20 的其他 OS、错误包、开机自启等未由本轮覆盖。 |
| T18 披露和受管控制 | 两账户分别确认当前策略，并可各自暂停/恢复 Web 控制 | 两账户的 Web 披露和本机授权完成。 | 披露确认完成；暂停/恢复、旧副本和交叉影响未执行。 |
| T04 首轮拒绝 | Alice 申请，Bob 独立拒绝，两端不形成连接 | Alice Web 发起 `friend-c9c2753e15ee311bbe9c1d61b2f9593584d9f992`，Bob 本人拒绝；Alice 看到“已拒绝”，未显示已连接，认为文案清楚。 | 拒绝路径通过。 |
| T04 拒绝后再申请 | Alice 沿用同一准确 URN 重新申请，使用独立新请求；Bob 再独立接受 | Alice 在 Web 新建第二个同 URN 联系人返回 `invalid_params`。改由 Bob 发起反方向独立请求 `friend-5f40d4d9fa5ccdc441adba95d5ac207497bedfa9`，Alice 本人接受；两端最终 `connected`。 | 原定 Alice 重申请路径失败；反方向替代路径通过，不能抵消原缺陷。 |
| T05 Alice Web → Bob 原生 → Alice Web | Bob 原生 Hermes 看见准确来信和提醒，独立回复；Alice Web 按准确消息 ID 阅读并关闭提醒 | Alice Web 发 `message-783190c9-bc10-4882-8f78-a7cba00ee36a`。Bob 原生 Hermes 本人看到准确来信；通过原生问题卡 `approval-bfcf1c753359874892a19010048a720c` 本人确认，回复 `t05-b-ack-bob-001` 到达 Alice。Bob Web 将 Alice 原信标为已读、Agent 已记录；Alice Web 最终也将 Bob 原生回复标为已读。 | 这一路真人操作和收件状态完成；曾发生卡片识别歧义，详见下文。 |
| T05 Bob Web → Alice 原生 → Bob Web | Alice 原生 Hermes 看见第二方向的来信并回复，Bob Web 阅读该回复 | Bob Web 发 `message-93c01ca4-0b2f-4a87-b5fa-8cf984caeebf`；Alice Web 看见并标为已读。 | 传输与 Alice Web 可见完成；Alice 原生阅读/回复、Bob 再阅读尚未执行，不能判定整条路径通过。 |
| T06 双边协作 | 两端独立 scope、invite/join、同版各自接受或独立拒绝；不创建日历 | 真人尚未操作 T06。隔离 Alice venv 中 `python/tests/test_collaboration_v2.py` 的 29 项本地协议测试通过。 | 真人决定和 Web 审批体验未测试；后续生产合成协议结果见下表。 |
| T07 真实模型对话 | Web 提交后原 turn 最终 `completed`，回复只用一句中文并含指定标记 | Alice 本人发送。原 turn `turn-5bb3ebbe0f6078dd7ae64d0d286e86e9e663a65f`、会话 `85957f6b-ee37-4218-82a0-2ffe1309816a` 在 Alice 本机 Store 为 `completed`、回复非空且无错误；测试者看到最终回复含 `T07-A1-7mYCMiYA` 并报告页面显示已完成。回复包含额外个人画像建议，且超过要求的一句。 | 真实模型回合完成；提示遵循/答复质量有偏差。测试者未单独评价状态变化是否易懂。 |
| T10 系统通知 | Bob 启用后台 Push，收到固定、无私信正文的 Windows 通知；点击只进入提醒中心 | Bob 本人在 Edge 启用后台推送、点击“发送测试提醒”，亲眼看到系统通知；点击进入提醒中心。测试者确认通知没有私人消息正文，也没有自动接受任何请求。 | 真人系统通知段通过；320/390px 真人窄屏、键盘体验和真实手机未执行。 |

T04 真人阶段失败的直接原因是 Agent 对已拒绝联系人的准确 URN 保留绑定，而当时生产 Web 只提供“新增联系人”，新建另一个 `contact_id` 会触发参数校验失败。正确重申请需要从原已拒绝联系人发起，沿用原 `contact_id`、别名和 URN，再产生独立的新好友请求。反方向发起后的 `connected` 仅用于继续真人 T05，不证明 Alice 当时遇到的 UI 缺陷已消失。后续 Web 修复已部署，**使用另一对 Codex 合成身份**完成了线上重申请复测；真人阶段的失败事实仍保留。

T05 的两条 Bob→Alice 消息时间相近、来自同一个 URN。Alice 曾报告已将原生回复 `t05-b-ack-bob-001` 标为已读，但随后按本机 Store 的消息 ID 对账，实际先读的是较新的 Bob Web 消息 `message-93c01ca4-0b2f-4a87-b5fa-8cf984caeebf`，原生回复当时仍未读。测试者刷新后只点击原生回复自己的按钮；最终**两条均已读，各自提醒均已解决**。目前证据说明卡片区分不够直观；没有证据表明 Web 把点击的一条错误写到另一条消息 ID。后续 Web 修复已显示可复制的精确消息 ID，并让已读按钮的可访问名称包含该 ID；合成 T05 已核对按 ID 标记已读时另一条仍未读。

## 之后的隔离自动浏览器与协议阶段

测试者随后明确要求 Codex 自己执行后续流程，并按“先预设效果，再测试看是否符合预期”推进。因此从这一时点起，自动动作限定在 **Codex 新建、持有的隔离合成账户、浏览器 profile 和 Agent 身份**；其结论标为自动化功能或界面机制验证，不倒填为上述真人 Alice/Bob 的决定、亲眼所见或主观理解。自动判据在继续测试前写入忽略提交的 `build/live-two-agent-acceptance/live-20260925-ux-hitl-01/automated-expectations.md`。桌面内置 Computer Use 控制因无法可靠验证当前浏览器 URL 而停止；后续浏览器入口采用隔离 Playwright Chromium，不能称为内置浏览器已通过。

自动运行号 `live-20260925-codex-web-auto-02`：两套全新 Windows Hermes/helper profile、独立 Store 和 Web 账户，helper 分别监听 `63525`、`63546`；Alice URN `urn:agent-comm:agent:GNQW7XrCMGctnZbbvM3Ew8`，Bob URN `urn:agent-comm:agent:QPjQsp3x2f3JNbtXhsy9Sy`。隔离 Playwright Chromium 已为两账户完成生产 Web 注册、策略确认和一次性连接领取，`browser-claim.json` 中两端均为 `policy_verified=true`、`claim_completed=true`。随后分别读取已验签的精确 epoch 3 披露并只对这两个 Codex 持有的身份执行本机授权，读回均为 `v2_send_ready=true`。密码、票据、模型密钥和浏览器存储状态只存放在忽略提交的私有文件，不在此报告披露。

**首次生产 Web 更新。** Web 提交 `0dfab7b`（拒绝后重申请、收件箱精确消息 ID）构建为镜像 ID 前缀 `d6fdc288` 并部署；原镜像保留回滚标签。更新前对 Web SQLite 做在线备份，备份 `quick_check=ok`，相关行数部署前后均为 `46`；Web 更换后 nginx 已 reload，健康检查返回 HTTP 200。这些是部署与存储检查，不单独证明业务用例通过；下面的 T04～T07 是在此线上 Web 版本上另行执行的合成验收。

**第二次生产 Web 更新。** T10 初测发现窄屏溢出后，提交 `6562a92`（长政策标识换行）构建为镜像 ID 前缀 `9854fa7b` 并部署。第二次 Web SQLite 在线备份 `quick_check=ok`；部署后 `nginx -t`、reload、HTTPS `/login` 与 `/healthz` 均正常，两个端点返回 HTTP 200。Web 服务按发布流程重建且重启计数为 0，Platform 容器未重建；四份 SQLite `quick_check=ok`。随后在该线上版本执行下表的 T10 复测；部署成功本身不作为窄屏通过证据。

| 后续用例 | 预先固定的客观判据 | 生产自动结果及忽略提交的证据 | 结论与边界 |
| --- | --- | --- | --- |
| T04 拒绝后同绑定重申请 | 拒绝态才显示入口；先确认准确 URN，再沿用原绑定只提交一次；旧拒绝、新请求各自保留，外部旁路请求不能消除不确定状态 | `browser-t04.json` 为 `PASS`：首请求 `friend-1647148799fa17dc4c631a0e9803e929731d7311` 被拒绝；两步确认后沿用原绑定 `contact-3e6fce9d-9769-4c21-8ef3-7a40161fc1ae`，新请求 `friend-eb0c2bd21b61bd60cc4fc5f1a8a2ce04b9e573ff` 被接受；旧拒绝保留，双方 `connected`，页面错误 0。 | `0dfab7b` 生产合成浏览器路径通过；不改变原真人阶段的失败记录。 |
| T05 双向消息和精确已读 | 双方消息有唯一 ID、准确发送/接收者及持久收件；对一个 ID 标已读不影响另一条 | `browser-t05.json` 为 `PASS`：Alice→Bob 两条 `message-c79021ec-9000-4ce2-b971-ef81a948bb0f`、`message-d31fe0a2-0f03-4faa-a8d6-28c56cdd8b28`，Bob→Alice 一条 `message-ddde7155-a1d9-4e96-9a69-49857c2da3f7`；发送者、收件人和正文匹配，按消息 ID 已读隔离、对应提醒关闭，刷新无重复卡片，页面错误 0。 | `0dfab7b` 生产合成浏览器与两端 Agent 状态通过；不补足原真人 Alice Hermes 回信体验。 |
| T06 双边协作 | 双方独立 scope 和决定；同版双方接受才成约；独立拒绝不成约；无日历写入 | `t06-expectations.json` 在执行前固定判据；`t06-result.json` 为 `PASS`。拒绝轮 Bob 独立拒绝、无双方约定；接受轮双方同步 `agreement-766db25e7b31a3d650427244a62ed6c9a1e4043b`，`calendar_created=false`。`t06-web-readonly.json` 为 `PASS`：双方生产 Web“事项”只读显示拒绝轮“部分接受，等待另一方”、接受轮“双方已同步约定”，无业务写入。 | 生产合成双端协议和 Web 只读呈现通过；决定由合成 owner driver 作出，未测试真人理解、Hermes 原生问题卡或 Web 审批按钮。 |
| T07 模型与刷新 | 原 turn 完成、回复非空、刷新后不重复执行 | `browser-t07.json` 为 `PASS`：真实 MiniMax 回复与要求的 `T07-AUTO-live-20260925-codex-web-auto-02` 标记完全一致；`turn-378e1df612e22584d48f815e0ce2f848f326894b` 为 `completed`，刷新后仍只有这一条 turn，页面错误 0。 | `0dfab7b` 生产合成 Web→Hermes 模型回合通过；真人阶段的答复质量偏差仍独立保留。 |
| T10 窄屏、键盘和通知导航 | 320/390px 无横向溢出，键盘焦点和操作有效；通知点击只到提醒中心 | 首轮 390px 测到长政策 Platform ID 使 `main` 宽达 509px，判失败。`6562a92` 上线后，`browser-t10.json` 为 `PASS`：完整政策加载后，所测联系人 320/390、收件箱 320/390、事项 320、提醒中心 320/390 的 `document`、`main`、`content` 宽均等于视口；键盘焦点与 ArrowRight/End/Home、Enter/Space 路径可用；提醒深链只导航，无私人正文，页面错误 0。最终截图为 `browser-t10-*.png`。 | `6562a92` 生产隔离 Chromium 所测自动场景通过。无真人 320/390 或手机体验结论；真实 OS 横幅仍仅由先前 Bob Edge 真人操作证明，headless Chromium 未声称观察到横幅。 |

**T18 首两轮自动尝试。** `t18-expectations.json` 在操作前固定了两账户各自暂停、已保存内容仍可读、新控制与同步返回 409、对方账户继续可用、恢复后重新可用及账户隔离等判据。在 `6562a92` 线上 Web 上，两轮功能断言均符合预期，结束时均核对 Alice/Bob 已恢复；但浏览器偶发 React #418 页面错误，故 `t18-first-attempt.json` 和第二轮 `t18-result.json` 均判 `FAIL`。只读对照证明同一首屏时间在 UTC 服务端为 `9/25 15:58`、上海时区浏览器为 `9/25 23:58`；`displayTime` 使用环境默认时区导致水合文本不一致。此问题早于本轮 Web 改动，不影响“暂停后 409”的功能事实，但在消除页面错误并线上复测前不能判 T18 通过。在途响应、MQ ACK、证书撤销等故障注入未纳入本轮生产状态操作。

**第三次生产 Web 更新与 T18 复测。** Web 提交 `c4a0e89` 让服务端及浏览器首轮水合都显示稳定的“本地时间加载中…”，水合后按浏览器本地时区显示，并稳定化首屏在线/审批到期状态。UTC 服务端配 UTC、上海 Chromium 的真实 Next 回归分别覆盖连接、事项、提醒和原报错的收件箱，均无页面错误；构建与 198 项单元测试通过。部署前在线备份 Web SQLite 至服务器 `/root/agent-comm-releases/2026-09-26-web-hydration-c4a0e89/web-prod-preimage.sqlite`，`quick_check=ok`，旧镜像保留 `rollback-pre-hydration-20260926` 标签。本机 Linux/amd64 镜像 ID 前缀 `91054133` 的源码标签匹配完整 Web 提交；上传包 SHA-256 为 `d7d56a4da2d2d60ac4cb4a318953b73cf7b736bbaa7a821b0718564d48fb06d4`，服务器校验一致。根仓库提交 `46fa8c6` 固定新 Web；服务器只重建 Web 并 reload nginx，Platform 容器未重建。公网 `/login`、`/healthz` 返回 200，Web/Platform 重启计数均为 0，Web、Registry、MQ、Audit 四库 `quick_check=ok`。

`t18-post-hydration-01-expectations.json` 在复测前单独固定了页面零错误、两账户独立暂停与恢复的判据。新线上版本的只读 `t18-post-hydration-01-hydration-smoke.json` 为 `PASS`：Alice/Bob × 收件箱/联系人/事项 × UTC/上海共 12 次观察，页面错误 0。`t18-post-hydration-01-result.json` 为 `PASS`：Alice 和 Bob 分别暂停时，各自新控制、新同步返回 409，拒绝的控制 ID 查询为 404；已保存来信仍可见、刷新禁用；另一账户同步返回 202、新 `capabilities` 控制完成。各自恢复后新控制完成；跨账户读取 Agent 返回 404，原绑定不变。`finally` 核对两端均已确认且未暂停，无需补救恢复，页面错误 0。**T18 的生产合成浏览器暂停/恢复与隔离路径通过**；先前两轮失败仍保留，不倒写为通过，也不等同真人理解或在途故障注入。

**T20 当前发布包只读审计。** `t20-readonly-evidence.json` 记录 v0.9.1 四份已下载 ZIP（Windows amd64、Linux amd64、macOS amd64/arm64）的 SHA-256 与字节数均匹配外部发布清单，四份解包的 `install.py --check-only` 文件与 wheel 检查通过；在 Windows 不加跨平台标志检查 Linux 包时明确 exit 2。两套全新隔离 Windows Hermes/profile/helper 已完成各自首装、一次性连接与生产浏览器领取。其他 OS 的原生安装/升级、系统开机自启、这两端的就地升级与状态保持、当前 v0.9.1 错误或篡改包负向测试和服务器严格权限检查，本轮尚未执行。

**T06 主动审批卡的预设与复测。** 在另一次隔离合成验收中，先固定 `t06-webcards-20260926-01` 的判据：审批卡须让主人直接核对当前方案编号、版本、主题、双方完整 URN、UTC 起止时间、条款摘要及不创建日历的边界，网页问题须与 Agent Store 的问题一致，另一账户不可见；Alice 单方接受不成约，Bob 拒绝不发送接受消息，双方分别接受同版后才形成相同约定。线上 Web 配旧 Runtime 的第一张 Alice 卡 `approval-23c6c996d5f2937f0fdcb17d18ce5b26` **失败**：`t06-webcards-20260926-01-reject-alice-observe.json` 显示卡片只有接受事件的摘要，没有方案编号、版本、主题和时段。未点击该缺条款卡。SDK 候选版补足从本机已核对方案生成的完整问题，并使旧摘要卡失效；旧卡的 Store 审批判断为 false，Bob 接受消息数为 0。随后按事先固定的 `t06-webcards-20260926-02-expectations.json` 用全新协作/提议/审批 ID 在**线上 Web/Platform + 本机合成 Alice/Bob 候选 SDK**重跑。四张 Web 审批卡均显示完整条款，问题与对应 Store 一致、跨账户隔离；四次网页操作的页面错误总数为 0。拒绝轮 Alice 单方接受后 Bob 在自己账户拒绝，Bob 接受消息数为 0、两边无约定；同意轮 Alice/Bob 各自点击一次同意，双方得到相同 `agreement-67358c55a8fab3d5232b1d0a353568c65399b8bc`，状态为 `agreement_only_complete`，`calendar_created=false`。证据为 `t06-webcards-20260926-02-final-audit.json` 及各角色的 observe/intent/web/progress JSON。旧卡持久失效、正式发布包升级后的再验证另列，不把候选 wheel 视作已公开发布。

**T05 原生反向消息的预设与闭环。** 另一次合成 Bob Web → 合成 Alice 原生 Hermes 测试先固定 `t05-native-expectations.json`，Bob Web 只发送一次 `message-dec813e2-3c4c-492e-920e-36b3bb29822f`。Alice 独立 Hermes Dashboard 的原生聊天通过 `agent_comm_collaboration` 看到准确发送者 URN、消息 ID 和正文。为避免与 T06 同时操作，第一次在模型仍“计算中”时暂停，见 `t05-native-stop.json`。续测最初因终端画布/DOM 文本读取差异和匹配错误提示词而误判“没有卡”；截图与 Store 证明真实原生确认卡已出现，没有业务失败或出站。核对同一 `approval-3692179504320f45c512c623607c2d97` 的准确 Bob URN 与全文后，合成 Alice 只在该卡输入“同意”，唯一回复 `message-6e2e4736e1ed9753643985bf4c7371914cde4199` 由 Bob Agent 持久收到；Bob Web 按该 ID 标为已读、相应提醒已解决、刷新无重复。最终 `t05-native-final-audit.json` 记为 `PASS_WITH_MODEL_DUPLICATE_PROPOSAL_DENIED`：模型在前一张卡超时后另准备了一张同正文但未发送的卡 `approval-4e92b20e7d3a7824b603d96e673f9653`，Codex 在合成 Alice Web 核对后明确拒绝，最终没有第二条出站或入站。此结果补足**合成**原生反向链路，不倒填为真人 Alice 已回复。

**T06 正式发布包的原位升级与线上复测。** [GitHub 正式 v0.9.2 Release](https://github.com/BillShiyaoZhang/agent-comm/releases/tag/v0.9.2) 的 Windows ZIP SHA-256 为 `4bd338b8de5f0980c98b38dc27df76af8ac98ab6f85a3d7878aa0c4fc6e7f238`，两份 wheel 与正式资产一致。`sdk-official-20260926/expectations.json` 在安装前冻结原身份、数据库、配对、旧审批和新 Web 卡的判据；先对 Alice/Bob 各五个 SQLite 在线备份、各六份静态身份文件备份，安装器在两端 `--check-only` 通过，再仅在两个合成 venv 重装正式 wheel 并顺序重启各自 Gateway。两端最终 connected/idle，正式包导入路径、身份、配对、T05 精确消息、已有 T06 协作均保持；旧摘要卡在 SQLite 仍为 `superseded`、对应操作为 `denied`。首次 Alice Gateway 由默认沙箱启动时无法读取该合成 profile 的插件文件，出现 disconnected；改用有读取权限的同一合成环境精确重启后恢复，未回滚正式 wheel、未碰真人 Hermes。`sdk-official-20260926/summary.json` 与前后审计记录此顺序。

正式包上另用全新 ID 各跑一轮线上 Web 审批。`t06-official-20260926-01-final-audit.json` 为拒绝轮：双方卡片均显示当前完整条款、与各自 Agent Store 一致且另一账户不可见，Alice 同意后 Bob 在自己卡中拒绝；两边无 Bob accept 消息、无 agreement、无日历事件，页面错误 0。`t06-official-20260926-02-expectations.json` 在接受轮前另行冻结判据，`t06-official-20260926-02-final-audit.json` 为 `PASS`：Alice 单独同意时两边 agreement 仍为空；Bob 对同一版独立同意后双方得到同一个 `agreement-edabd790c646517d7b78745f7ebc2444966b796d`，状态 `agreement_only_complete`、`calendar_created=false`，两张卡条款完整、跨账户隔离、页面错误 0。两端 Gateway 最终连接且空闲。**正式发布包在这两套合成身份上的原位升级与 T06 Web 双分支通过**；其他 OS 原生安装、真人理解和真实会议举行不在此结论内。

T10、T18 均保留“首轮失败、修复后线上复测通过”的时间顺序。尚未补做的真人步骤包括 T05 的 Alice 原生 Hermes 阅读/回复及 Bob 再阅读（05-A3/05-B3）、T06 真人对审批文字的理解、真人 320/390px 与手机窄屏操作；合成 owner 的批准不能代替这些真人结论。
